
node {
    stage('Checkout repo') {
        checkout scm
    }

    stage('Plan') {
        // TODO configure terrafrom plan
    }

    stage('Apply') {
        // TODO configure terraform apply if approved
    }
}
